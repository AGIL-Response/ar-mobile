export type IncidentCoordinate = {
  id: string;
  coordinates: [number, number];
};


export type UserCoordinate = {
  id: string;
  avatarId: string;
  status: string;
  coordinates: [number, number];
};