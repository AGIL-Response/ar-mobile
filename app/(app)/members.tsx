/**
 * Members Screen
 * Full members list grouped by role
 */

import React from 'react';

import { MembersScreen } from '@/screens/members';

export default function MembersRoute(): React.JSX.Element {
  return <MembersScreen />;
}
